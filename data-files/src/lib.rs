pub mod domain;

/* pub fn generate_characters(descriptions: Vec<CharacterDescription>) {
    for description in descriptions {
        let character = description.apply_transformations();
        println!("{:?}", character);
    }
}
 */
